
# Smart Assets – Streamlit App (AR/EN)

نظام ذكي مبسّط لإدارة الأصول باستخدام **Streamlit + Pandas + Plotly**،
مع دعم البحث الذكي العربي، لوحات تحكم، ومساعد تحليلي.

## 📦 المجلدات والملفات

```
asset-agent-streamlit/
├─ smart_assets_app.py            # ملف التطبيق الرئيسي
├─ requirements.txt               # الاعتمادات
├─ utils/
│  └─ data_loader.py              # دالة تحميل بيانات الأصول (Excel/Google Sheets)
├─ data/
│  └─ SGS_AutoGPT_Assets_Template_MoF.xlsx  # قالب الأصول (وزارة المالية)
└─ .streamlit/
   └─ config.toml                 # إعدادات الواجهة والثيم
```

## ▶️ تشغيل محليًا
```bash
pip install -r requirements.txt
streamlit run smart_assets_app.py
```

## ☁️ النشر على Streamlit Cloud
1. أنشئ مستودع جديد في GitHub (عام أو خاص).
2. ارفع جميع ملفات هذا المشروع إلى المستودع.
3. اذهب إلى https://share.streamlit.io واربِط حسابك بـ GitHub.
4. اختر المستودع + الفرع + **smart_assets_app.py** كملف التطبيق.
5. (اختياري) أضف أسرار (Secrets) في **Streamlit Cloud → App → Settings → Secrets** مثل:
   ```toml
   GOOGLE_SHEET_URL = "https://docs.google.com/spreadsheets/d/..."
   ```
   ثم في `data_loader.py` استبدل مسار الملف بـ `st.secrets.get("GOOGLE_SHEET_URL")` لو رغبت.

## 🗂️ بيانات الأصول
- الافتراضي يستخدم ملف الإكسل الموجود في `data/SGS_AutoGPT_Assets_Template_MoF.xlsx`.
- تقدر تغيّر المسار في `utils/data_loader.py` أو تمرره من التطبيق.

## 🔐 ملاحظات أمان
- لا ترفع ملفات حساسة علنًا.
- استخدم `secrets.toml` للمفاتيح والروابط الحساسة.

---

**By Saad & GPT 🤝**
